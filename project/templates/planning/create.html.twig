{% block body %}

	<h1>
		Créer un nouveau cours
	</h1>

	{{form_start(form)}}
	<div>
		{{ form_row(form.name)}}
		{{ form_row(form.teacher)}}
	</div>
	<div>
		{{ form_row(form.room)}}
		{{ form_row(form.level)}}
	</div>


	{{form_end(form)}}

	<a href="{{ path('home') }}">Retour à l'index</a>
{% endblock %}
